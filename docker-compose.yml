services:
  frontend:
    build:
      context: .
      args:
        VITE_API_BASE_URL: "/api/v1"
    ports:
      - "3000:80"
    extra_hosts:
      - "host.docker.internal:host-gateway"
    restart: unless-stopped
